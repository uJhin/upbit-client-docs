<h1 id='install'>Install (설치)</h1>
<blockquote>
<p>Command</p>
</blockquote>
<div class="highlight"><pre class="highlight shell tab-shell"><code><span class="nv">$ </span>pip <span class="nb">install </span>upbit-client
<span class="nv">$ </span>git clone https://github.com/uJhin/upbit-client.git
<span class="nv">$ </span>git clone https://github.com/uJhin/python-upbit-client.git
</code></pre></div>
<p>설치는 <code>git</code> 커맨드를 사용하여 다음과 같이 진행합니다.</p>

<p><code>git clone https://github.com/uJhin/upbit-client.git</code></p>

<p>Python 패키지는 아래와 같이 진행합니다.</p>

<p><code>git clone https://github.com/uJhin/python-upbit-client.git</code></p>

<p>혹은 <code>pip</code> 커맨드를 사용하여 다음과 같이 진행합니다.</p>

<p><code>pip install upbit-client</code></p>
